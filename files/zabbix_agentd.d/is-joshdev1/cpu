# This file should be a symbolic link that points to the ../authConf/cpu file

# Run this in the command line. That way the symbolic link will point to the cpu file in authConf.

# ln -s ../authConf/cpu .

# Don't use this file, delete it and make a symbolic link.
